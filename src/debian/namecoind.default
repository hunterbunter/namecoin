# Defaults for namecoind initscript
# sourced by /etc/init.d/namecoind
# installed at /etc/default/namecoind by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Set this option to no as soon as you have configured /etc/namedoind/namecoin.conf
UNCONFIGURED="yes"
